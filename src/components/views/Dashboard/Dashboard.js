import React from 'react';
import styles from './Dashboard.module.scss';

const Dashboard = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>Strona Główna</h2>
  </div>
);

export default Dashboard;